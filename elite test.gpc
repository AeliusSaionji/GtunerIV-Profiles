#pragma METAINFO("elite test", 1, 0, "<author_name>")

#include <remapper.gph>
#include <xb1.gph>

#define null_paddle(x, y) set_val(x, is_active(y) ? 0.0 : get_val(x))

main {
    null_paddle(XB1_P1, XB1_LT);
    null_paddle(XB1_P2, XB1_LT);
    null_paddle(XB1_P3, XB1_LT);
    null_paddle(XB1_P4, XB1_LT);
}